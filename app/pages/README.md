# 📄 Страницы приложения (app/pages/)

Папка содержит две основные страницы Streamlit приложения: анкету и симуляцию.  

## 📁 Структура
```
pages/
├── application.py # Анкета с эскалацией
└── simulation.py # Симуляция следующих 24 часов
```
## 📄 Описание страниц  

### `application.py` - Анкета  

Страница для работы с одной заявкой.    
Пользователь заполняет данные, настраивает параметры и получает решение с полной интерпретацией.  

**Основные блоки:**  

#### 1. Ввод данных
- Пошаговая форма (сначала общие вопросы, потом точные значения)  
- Гибкие способы ввода (слайдеры для быстрого тестирования, точные значения для реалистичных сценариев)  
- Автоматический расчет `DebtRatio` на основе дохода и платежей  

#### 2. Настройки (сайдбар)
- **Порог одобрения** (0.3-0.7)  
- **Отступы уверенности** для LR и второй модели (задают "серую зону" вокруг порога одобрения)  
- **Выбор модели** для эскалации (`XGBoost`/`LightGBM`/`CatBoost`/`Random Forest`)  
- **Статистика** по всем обработанным заявкам  

#### 3. Эскалация
Вызов `escalation_decision()` из `app/models/escalation.py`:
1. Бизнес-правила (возраст, коды 96/98, экстремальные значения)  
2. `Logistic Regression`   
3. Сложная модель (выбранная пользователем)  
4. Ручной разбор (если модели не уверены)  

#### 4. Интерпретация
- **Decision path** - видно всю цепочку принятия решения  
- **Logistic Regression** - анализ коэффициентов (вклад в логит) и marginal effects (влияние на вероятность)  
- **Сложные модели** - SHAP-анализ (для tree-based)  
- Графики вклада признаков  

### `simulation.py` - Симуляция работы системы с обратной связью в течение следующих 24 часов с момента запуска  

Страница для массовой симуляции работы системы в течение суток.   
Использует неразмеченный датасет с Kaggle (~100 тыс анкет) для генерации реалистичного потока заявок.  

**Основные компоненты:**  

#### 1. Генерация трафика (`simulation/core/traffic_generator.py`)
- Реалистичное распределение заявок по времени суток  
- Утренний пик (13:00), вечерний пик (19:30), послеобеденный спад  
- Возможность добавления импульсов (рекламные кампании)  

#### 2. Обработка очередей (`simulation/core/processor.py`)  
- Две очереди: бизнес-правила и модели  
- Отдельные пулы специалистов (основные) и экспертов (бизнес-правила)  
- Время обработки специалистом зависит от уверенности модели   

#### 3. PID-регулятор (`simulation/controllers/pid.py`)  
- Автоматически регулирует отступы моделей    
  (те "серую зону" вокруг порога одобрения, попав в которую, анкета отправляется на уровень выше или на ручной разбор)  
- Поддерживает целевую загрузку специалистов (по умолчанию 80%) в течение всего времени симуляции  
- Адаптация под пиковые нагрузки (за счёт регулирования отступов, сглаживает пики)  
  

#### 4. Визуализация
- **Графики** (`simulation/visualization/plots.py`): очереди, загрузка, поток, параметры PID  
- **Анимация** (`simulation/visualization/animation.py`): heatmap специалистов, динамика очередей, статистика  

**Настраиваемые параметры:**  
- Количество специалистов и экспертов  
- Базовое время обработки специалистами и экспертами  
- Целевая загрузка  
- Порог одобрения  
- Начальные отступы  
- Коэффициенты `PID`  

## 🚀 Навигация

Страницы вызываются из `main.py`:
```python
# main.py
if st.button("Перейти к анкете"):
    st.switch_page("pages/application.py")

if st.button("Перейти к симуляции"):
    st.switch_page("pages/simulation.py")
```

## 🔧 Используемые модули

application.py → models/escalation.py, models/interpretation.py, utils/data_loader.py  
simulation.py → simulation/core/, simulation/controllers/, simulation/visualization/, utils/data_loader.py   

📚 Подробнее  
[Логика эскалации](../models/escalation.py)  
[Интерпретация](../models/interpretation.py)  
[Ядро симуляции](../simulation/core/README.md)  
[Утилиты](../utils/README.md)  
