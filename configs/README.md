# ⚙️ Конфиги Hydra (configs/)

Папка содержит конфигурационные файлы для управления экспериментами через **Hydra**.  
Позволяет гибко менять параметры моделей, препроцессора и обучения без изменения кода.  

## 📁 Структура  
```
configs/  
├── experiment/ # Конфиги конкретных экспериментов  
│ ├── lgbm_experiment.yaml
│ ├── logreg_experiment.yaml
│ ├── xgb_experiment.yaml
│ ├── rfc_experiment.yaml
│ ├── catboost_experiment.yaml
│ └── dtc_experiment.yaml
├── model/ # Базовые параметры моделей
│ ├── lgbm.yaml
│ ├── logreg.yaml
│ ├── xgb.yaml
│ ├── rfc.yaml
│ ├── catboost.yaml
│ └── dtc.yaml
└── preprocessor/ # Настройки препроцессора
└── default.yaml
```
## 🎯 Как это работает

### Композиция конфигов  
Каждый эксперимент собирается из нескольких файлов через `defaults`:   
Пример:  
```yaml
# configs/experiment/lgbm_experiment.yaml
defaults:
  - /preprocessor: default   # базовые настройки препроцессора
  - /model: lgbm             # базовые параметры модели
  - _self_                   # текущий файл (может переопределять)
```
Приоритет (от низшего к высшему):  
1. Базовые параметры из /preprocessor/default.yaml  
2. Базовые параметры из /model/lgbm.yaml  
3. Переопределения в самом lgbm_experiment.yaml  

## 🔧 Добавление новой модели

1. Создать configs/model/новая_модель.yaml с базовыми параметрами  
2. Создать configs/experiment/новая_модель_experiment.yaml с:  
```
yaml
defaults:
  - /preprocessor: default
  - /model: новая_модель
  - _self_
```
3. Добавить специфичные для эксперимента переопределения

📚 Подробнее  
Обучение моделей  
Эксперименты ClearML  
Streamlit приложение  
