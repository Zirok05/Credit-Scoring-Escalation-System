# 🔧 Препроцессоры (preprocessors/)

Папка содержит обученные препроцессоры для преобразования сырых данных в финальные признаки.  
Препроцессоры обучены скриптом src/train_full_dataset.py на полном датасете (150k строк)   
с удалением признаков "код 96" и "код 98".  

## 📁 Структура
```
preprocessors/
├── preprocessor_150.pkl # CreditDataPreprocessor (обучен на всех данных)
└── scaler_150.pkl # CreditScaler (standard scaling)
```

## 🎯 Назначение

Эти файлы используются в **Streamlit приложении** для обработки пользовательских анкет:
1. `preprocessor` - создает фичи, обрабатывает пропуски, выбросы, признаки "код 96" и "код 98" удаляет
2. `scaler` - масштабирует признаки c помощью StandardScaler из sklearn

## 🔧 Как использовать

```python
import joblib
import pandas as pd

# Загрузка
preprocessor = joblib.load('preprocessors/preprocessor_150.pkl')
scaler = joblib.load('preprocessors/scaler_150.pkl')

# Применение к сырым данным
raw_data = pd.DataFrame({...})  # анкета от пользователя
processed = preprocessor.transform(raw_data)
scaled = scaler.transform(processed)
```

📚 Подробнее
- [Код препроцессора](../src/utils/credit_preprocessor.py)
- [Обучение моделей](../src/README.md)
